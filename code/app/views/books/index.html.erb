<!DOCTYPE html>
<html>
<head>
<style>
table, th, td {
  border: 1px solid black;
  border-collapse: collapse;
}
</style>
</head>
<body>


<h1>My Bookshelf</h1>

<%= link_to 'Homepage', welcome_index_path %><br> 
<%= link_to 'New Book',  new_book_path %><br>

<table style="width:100%">
  <tr>
    <th>Title</th>
    <th>Author</th>
    <th>Genre</th>
    <th>Status</th>
    <th></th>
  <tr>

  <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.author %></td>
      <td><%= book.genre %></td>
      <td><%= book.progress %></td>
      <td><%= link_to 'Show', book_path(book) %></td>
      <td><%= link_to 'Edit', edit_book_path(book) %></td>
      <td><%= link_to 'Delete', book_path(book),
              method: :delete,
              data: { confirm: 'Are you sure? This action cannot be undone.' } %></td>
    </tr>
  <% end %>
</table>

</body>
</html>
